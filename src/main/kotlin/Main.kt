import arcade.intro.Exercise09

fun main() {
    /*
    val ex = Exercise09()
    val list: MutableList<String> = mutableListOf(
        "aba",
        "aa",
        "ad",
        "vcd",
        "aba"
    )
    */
    //"lsads".forEach { println(it) }
    var str: String = "dsadfkljds"
    var pos: Int = str.indexOf("z")
    println(pos)
    //str = str.removeRange(pos, pos + 1)
    println(str)
    "dsadksaldjsak".sumOf { it ->
        var sum: Int = 0
        sum
    }
    //println(ex.allLongestStrings(list))
}